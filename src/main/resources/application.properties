spring.application.name=AirBnb

# DB Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/airbnb_db
spring.datasource.username=postgres
spring.datasource.password=suprit69
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.servlet.context-path=/api/v1
server.port=8080

# JWT Secret
jwt.secretKey=some-very-long-random-secret-string

# Frontend URL
frontend.url=http://localhost:3000

# Stripe Configuration
stripe.secret.key=sk_test_your_key_here
stripe.webhook.secret=whsec_your_webhook_secret_here

# Swagger Configuration
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
